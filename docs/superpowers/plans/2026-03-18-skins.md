# Skin System Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar um sistema de 4 skins visuais (Corporate, Noturno, Moderno, Clássico) com troca instantânea no header, persistência dual (localStorage + banco), e refatoração completa dos componentes para usar CSS variables.

**Architecture:** CSS Variables scoped por atributo `[data-skin="..."]` no elemento `<html>`, gerenciados por um `SkinContext` React. O `layout.tsx` raiz lê um cookie para aplicar a skin no SSR (anti-flash). A skin escolhida é salva em localStorage (cache) e no banco via `PATCH /api/consultants/me`.

**Tech Stack:** Next.js 14 App Router, Tailwind CSS, shadcn/ui, Supabase local (Docker), Vitest, Playwright

**Spec:** `docs/superpowers/specs/2026-03-18-skins-design.md`

---

## File Map

| Arquivo                                                           | Ação      | Responsabilidade                                                 |
| ----------------------------------------------------------------- | --------- | ---------------------------------------------------------------- |
| `src/app/globals.css`                                             | Modificar | Adicionar 4 blocos `[data-skin]` com CSS variables               |
| `src/lib/skin/types.ts`                                           | Criar     | `SkinId` union type, `SkinMeta` interface                        |
| `src/lib/skin/skins.ts`                                           | Criar     | Definições das 4 skins (metadados + paleta)                      |
| `src/lib/skin/skin-context.tsx`                                   | Criar     | `SkinProvider`, `useSkin()` hook, lógica de persistência         |
| `src/components/ui/skin-switcher.tsx`                             | Criar     | Dropdown no header com as 4 opções                               |
| `src/app/layout.tsx`                                              | Modificar | Tornar async, ler cookie `skin`, aplicar `data-skin` no `<html>` |
| `src/app/dashboard/layout.tsx`                                    | Modificar | Adicionar `<SkinSwitcher>` + refatorar hardcoded colors          |
| `src/app/dashboard/perfil/page.tsx`                               | Modificar | Adicionar seção "Aparência" com 4 cards                          |
| `src/app/api/consultants/me/route.ts`                             | Criar     | `PATCH` endpoint para salvar `theme`                             |
| `src/types/database.ts`                                           | Modificar | Adicionar campo `theme` em consultants Row/Insert/Update         |
| `supabase/migrations/20260318000001_add_theme_to_consultants.sql` | Criar     | Coluna `theme` com CHECK constraint                              |
| `src/components/auth/UserMenu.tsx`                                | Modificar | Refatorar dark: variants                                         |
| `src/components/dashboard/metric-card.tsx`                        | Modificar | Refatorar dark: variants                                         |
| `src/components/leads/lead-card.tsx`                              | Modificar | Refatorar dark: variants                                         |
| `src/components/leads/lead-list.tsx`                              | Modificar | Refatorar dark: variants                                         |
| `src/components/leads/lead-filters.tsx`                           | Modificar | Refatorar dark: variants                                         |
| `src/components/admin/admin-sidebar.tsx`                          | Modificar | Refatorar dark: variants                                         |
| `src/components/admin/stats-card.tsx`                             | Modificar | Refatorar dark: variants                                         |
| `src/components/billing/pricing-card.tsx`                         | Modificar | Refatorar dark: variants                                         |
| `src/app/dashboard/page.tsx`                                      | Modificar | Refatorar dark: variants (30+ classes)                           |
| `tests/unit/lib/skin/skin-context.test.tsx`                       | Criar     | Unit tests do SkinContext                                        |
| `tests/unit/components/ui/skin-switcher.test.tsx`                 | Criar     | Unit tests do SkinSwitcher                                       |
| `tests/unit/app/api/consultants/me/route.test.ts`                 | Criar     | Integration tests do PATCH endpoint                              |
| `tests/e2e/skin-system.spec.ts`                                   | Criar     | E2E: troca de skin + persistência                                |

---

## Task 1: CSS Variables — 4 skin blocks em globals.css

**Files:**

- Modify: `src/app/globals.css`

- [ ] **Step 1: Adicionar os 4 blocos `[data-skin]` ao globals.css**

Abrir `src/app/globals.css` e substituir o conteúdo atual por:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  /* Fallback no-JS: mantido para garantir que a página renderize sem JS */
  :root {
    --background: 0 0% 100%;
    --foreground: 222.2 84% 4.9%;
    --card: 0 0% 100%;
    --card-foreground: 222.2 84% 4.9%;
    --popover: 0 0% 100%;
    --popover-foreground: 222.2 84% 4.9%;
    --primary: 222.2 47.4% 11.2%;
    --primary-foreground: 210 40% 98%;
    --secondary: 210 40% 96.1%;
    --secondary-foreground: 222.2 47.4% 11.2%;
    --muted: 210 40% 96.1%;
    --muted-foreground: 215.4 16.3% 46.9%;
    --accent: 210 40% 96.1%;
    --accent-foreground: 222.2 47.4% 11.2%;
    --destructive: 0 84.2% 60.2%;
    --destructive-foreground: 210 40% 98%;
    --border: 214.3 31.8% 91.4%;
    --input: 214.3 31.8% 91.4%;
    --ring: 222.2 84% 4.9%;
    --radius: 0.5rem;
  }

  /* 1. Corporate — profissional, B2B, azul-marinho */
  [data-skin='corporate'] {
    --background: 0 0% 100%;
    --foreground: 222.2 84% 4.9%;
    --card: 0 0% 100%;
    --card-foreground: 222.2 84% 4.9%;
    --popover: 0 0% 100%;
    --popover-foreground: 222.2 84% 4.9%;
    --primary: 222.2 47.4% 11.2%;
    --primary-foreground: 210 40% 98%;
    --secondary: 210 40% 96.1%;
    --secondary-foreground: 222.2 47.4% 11.2%;
    --muted: 210 40% 96.1%;
    --muted-foreground: 215.4 16.3% 46.9%;
    --accent: 210 40% 96.1%;
    --accent-foreground: 222.2 47.4% 11.2%;
    --destructive: 0 84.2% 60.2%;
    --destructive-foreground: 210 40% 98%;
    --border: 214.3 31.8% 91.4%;
    --input: 214.3 31.8% 91.4%;
    --ring: 222.2 84% 4.9%;
    --radius: 0.5rem;
  }

  /* 2. Noturno — elegante, escuro, violeta */
  [data-skin='noturno'] {
    --background: 224 71% 4%;
    --foreground: 213 31% 91%;
    --card: 224 71% 7%;
    --card-foreground: 213 31% 91%;
    --popover: 224 71% 4%;
    --popover-foreground: 213 31% 91%;
    --primary: 263 70% 65%;
    --primary-foreground: 224 71% 4%;
    --secondary: 223 47% 11%;
    --secondary-foreground: 213 31% 91%;
    --muted: 223 47% 11%;
    --muted-foreground: 215 20% 65%;
    --accent: 216 34% 17%;
    --accent-foreground: 213 31% 91%;
    --destructive: 0 63% 31%;
    --destructive-foreground: 213 31% 91%;
    --border: 216 34% 17%;
    --input: 216 34% 17%;
    --ring: 263 70% 65%;
    --radius: 0.5rem;
  }

  /* 3. Moderno — startup, teal, cantos arredondados */
  [data-skin='moderno'] {
    --background: 0 0% 98%;
    --foreground: 240 10% 4%;
    --card: 0 0% 100%;
    --card-foreground: 240 10% 4%;
    --popover: 0 0% 100%;
    --popover-foreground: 240 10% 4%;
    --primary: 172 66% 36%;
    --primary-foreground: 0 0% 100%;
    --secondary: 172 30% 94%;
    --secondary-foreground: 172 66% 25%;
    --muted: 240 5% 96%;
    --muted-foreground: 240 4% 46%;
    --accent: 172 40% 90%;
    --accent-foreground: 172 66% 25%;
    --destructive: 0 84% 60%;
    --destructive-foreground: 0 0% 100%;
    --border: 240 6% 90%;
    --input: 240 6% 90%;
    --ring: 172 66% 36%;
    --radius: 0.75rem;
  }

  /* 4. Clássico — enterprise, roxo, creme */
  [data-skin='classico'] {
    --background: 40 20% 97%;
    --foreground: 270 30% 10%;
    --card: 40 15% 99%;
    --card-foreground: 270 30% 10%;
    --popover: 40 15% 99%;
    --popover-foreground: 270 30% 10%;
    --primary: 270 60% 38%;
    --primary-foreground: 0 0% 100%;
    --secondary: 270 20% 94%;
    --secondary-foreground: 270 60% 30%;
    --muted: 30 10% 93%;
    --muted-foreground: 270 15% 45%;
    --accent: 270 30% 90%;
    --accent-foreground: 270 60% 30%;
    --destructive: 0 84% 60%;
    --destructive-foreground: 0 0% 100%;
    --border: 270 20% 85%;
    --input: 270 20% 85%;
    --ring: 270 60% 38%;
    --radius: 0.375rem;
  }

  /* .dark mantido temporariamente para compatibilidade durante a refatoração */
  .dark {
    --background: 222.2 84% 4.9%;
    --foreground: 210 40% 98%;
    --card: 222.2 84% 4.9%;
    --card-foreground: 210 40% 98%;
    --popover: 222.2 84% 4.9%;
    --popover-foreground: 210 40% 98%;
    --primary: 210 40% 98%;
    --primary-foreground: 222.2 47.4% 11.2%;
    --secondary: 217.2 32.6% 17.5%;
    --secondary-foreground: 210 40% 98%;
    --muted: 217.2 32.6% 17.5%;
    --muted-foreground: 215 20.2% 65.1%;
    --accent: 217.2 32.6% 17.5%;
    --accent-foreground: 210 40% 98%;
    --destructive: 0 62.8% 30.6%;
    --destructive-foreground: 210 40% 98%;
    --border: 217.2 32.6% 17.5%;
    --input: 217.2 32.6% 17.5%;
    --ring: 212.7 26.8% 83.9%;
  }
}

@layer base {
  * {
    @apply border-border;
  }
  body {
    @apply bg-background text-foreground;
  }
}
```

- [ ] **Step 2: Verificar build**

```bash
cd E:/PROJETOS/Consultor.AI && npm run build 2>&1 | tail -5
```

Esperado: `✓ Compiled successfully` ou similar sem erros CSS.

- [ ] **Step 3: Commit**

```bash
git add src/app/globals.css
git commit -m "feat(skins): add 4 data-skin CSS variable blocks to globals.css"
```

---

## Task 2: Tipos e definições das skins

**Files:**

- Create: `src/lib/skin/types.ts`
- Create: `src/lib/skin/skins.ts`

- [ ] **Step 1: Criar `src/lib/skin/types.ts`**

```typescript
export type SkinId = 'corporate' | 'noturno' | 'moderno' | 'classico';

export const VALID_SKIN_IDS: SkinId[] = ['corporate', 'noturno', 'moderno', 'classico'];

export interface SkinMeta {
  id: SkinId;
  name: string;
  description: string;
  icon: string;
  /** Cor de preview para o mini-card nas configurações */
  previewBg: string;
  previewPrimary: string;
}

export function isValidSkinId(value: unknown): value is SkinId {
  return typeof value === 'string' && (VALID_SKIN_IDS as string[]).includes(value);
}
```

- [ ] **Step 2: Criar `src/lib/skin/skins.ts`**

```typescript
import type { SkinMeta } from './types';

export const SKINS: SkinMeta[] = [
  {
    id: 'corporate',
    name: 'Corporate',
    description: 'Profissional e confiável',
    icon: '🏢',
    previewBg: '#ffffff',
    previewPrimary: '#1e3a5f',
  },
  {
    id: 'noturno',
    name: 'Noturno',
    description: 'Modo escuro elegante',
    icon: '🌙',
    previewBg: '#060d1f',
    previewPrimary: '#9b72e8',
  },
  {
    id: 'moderno',
    name: 'Moderno',
    description: 'Clean e minimalista',
    icon: '⚡',
    previewBg: '#fafafa',
    previewPrimary: '#1a9e87',
  },
  {
    id: 'classico',
    name: 'Clássico',
    description: 'Tradicional e sóbrio',
    icon: '📜',
    previewBg: '#f7f4ee',
    previewPrimary: '#5e2d91',
  },
];

export const DEFAULT_SKIN_ID = 'corporate' as const;
```

- [ ] **Step 3: Commit**

```bash
git add src/lib/skin/
git commit -m "feat(skins): add SkinId types and skin definitions"
```

---

## Task 3: SkinContext e useSkin hook

**Files:**

- Create: `src/lib/skin/skin-context.tsx`
- Modify: `src/components/providers.tsx`

- [ ] **Step 1: Criar `src/lib/skin/skin-context.tsx`**

```tsx
'use client';

import { createContext, useCallback, useContext, useEffect, useState } from 'react';
import { DEFAULT_SKIN_ID, SKINS } from './skins';
import { type SkinId, isValidSkinId } from './types';

const STORAGE_KEY = 'consultor-ai-skin';
const COOKIE_NAME = 'skin';
const COOKIE_MAX_AGE = 365 * 24 * 60 * 60;

interface SkinContextValue {
  skinId: SkinId;
  setSkin: (id: SkinId) => Promise<void>;
  skins: typeof SKINS;
}

const SkinContext = createContext<SkinContextValue | null>(null);

export function SkinProvider({
  children,
  initialSkin,
}: {
  children: React.ReactNode;
  initialSkin?: SkinId;
}) {
  const [skinId, setSkinId] = useState<SkinId>(initialSkin ?? DEFAULT_SKIN_ID);

  // Aplica a skin no DOM
  useEffect(() => {
    const root = document.documentElement;
    root.setAttribute('data-skin', skinId);
    // Noturno precisa da classe .dark para compatibilidade com dark: variants remanescentes
    if (skinId === 'noturno') {
      root.classList.add('dark');
    } else {
      root.classList.remove('dark');
    }
  }, [skinId]);

  // Ao montar, lê localStorage (pode sobrescrever o SSR cookie se diferente)
  useEffect(() => {
    const stored = localStorage.getItem(STORAGE_KEY);
    if (stored && isValidSkinId(stored) && stored !== skinId) {
      setSkinId(stored);
    }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  const setSkin = useCallback(async (id: SkinId) => {
    setSkinId(id);

    // Cache local
    localStorage.setItem(STORAGE_KEY, id);

    // Cookie SSR anti-flash
    document.cookie = `${COOKIE_NAME}=${id}; path=/; max-age=${COOKIE_MAX_AGE}; SameSite=Lax`;

    // Persistência no banco
    try {
      await fetch('/api/consultants/me', {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ theme: id }),
      });
    } catch {
      // Falha silenciosa — skin já foi aplicada localmente
    }
  }, []);

  return (
    <SkinContext.Provider value={{ skinId, setSkin, skins: SKINS }}>
      {children}
    </SkinContext.Provider>
  );
}

export function useSkin(): SkinContextValue {
  const ctx = useContext(SkinContext);
  if (!ctx) throw new Error('useSkin must be used within SkinProvider');
  return ctx;
}
```

- [ ] **Step 2: Atualizar `src/components/providers.tsx` — adicionar SkinProvider preservando tudo**

O arquivo atual (`src/components/providers.tsx`) contém `ThemeProvider` (next-themes), `ReactQueryDevtools`, `initSentry`, e configuração completa do `QueryClient`. **Não remover nada.** Apenas adicionar `SkinProvider` e o prop `initialSkin`:

```tsx
'use client';

import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { ReactQueryDevtools } from '@tanstack/react-query-devtools';
import { ThemeProvider } from 'next-themes';
import { useState, useEffect, type ReactNode } from 'react';
import { initSentry } from '@/lib/monitoring';
import { SkinProvider } from '@/lib/skin/skin-context';
import type { SkinId } from '@/lib/skin/types';

export function Providers({
  children,
  initialSkin,
}: {
  children: ReactNode;
  initialSkin?: SkinId;
}) {
  useEffect(() => {
    initSentry().catch(console.error);
  }, []);

  const [queryClient] = useState(
    () =>
      new QueryClient({
        defaultOptions: {
          queries: {
            staleTime: 60 * 1000,
            gcTime: 5 * 60 * 1000,
            refetchOnWindowFocus: false,
            refetchOnReconnect: true,
            retry: 1,
            retryDelay: attemptIndex => Math.min(1000 * 2 ** attemptIndex, 30000),
          },
          mutations: {
            retry: 1,
            retryDelay: 1000,
          },
        },
      })
  );

  return (
    <QueryClientProvider client={queryClient}>
      <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
        <SkinProvider initialSkin={initialSkin}>{children}</SkinProvider>
        {process.env.NODE_ENV === 'development' &&
          process.env.NEXT_PUBLIC_ENABLE_REACT_QUERY_DEVTOOLS === 'true' && (
            <ReactQueryDevtools initialIsOpen={false} />
          )}
      </ThemeProvider>
    </QueryClientProvider>
  );
}
```

- [ ] **Step 3: Verificar TypeScript**

```bash
cd E:/PROJETOS/Consultor.AI && npm run type-check 2>&1 | tail -10
```

Esperado: `0 errors`.

- [ ] **Step 4: Commit**

```bash
git add src/lib/skin/skin-context.tsx src/components/providers.tsx
git commit -m "feat(skins): add SkinProvider and useSkin hook"
```

---

## Task 4: Migration + types

**Files:**

- Create: `supabase/migrations/20260318000001_add_theme_to_consultants.sql`
- Modify: `src/types/database.ts`

- [ ] **Step 1: Criar a migration SQL**

```sql
-- supabase/migrations/20260318000001_add_theme_to_consultants.sql
ALTER TABLE consultants
ADD COLUMN IF NOT EXISTS theme VARCHAR(20) NOT NULL DEFAULT 'corporate'
CHECK (theme IN ('corporate', 'noturno', 'moderno', 'classico'));
```

- [ ] **Step 2: Aplicar a migration no Supabase local**

```bash
cd E:/PROJETOS/Consultor.AI && npx supabase migration up
```

Esperado: `Applied migration 20260318000001_add_theme_to_consultants`.

> Se `supabase migration up` não funcionar no setup local com Docker, use:
>
> ```bash
> npx supabase db push
> ```

- [ ] **Step 3: Atualizar `src/types/database.ts` manualmente**

Localizar o bloco `consultants: { Row: {` e adicionar `theme: string;` após `subscription_tier`:

```typescript
// Row: adicionar
theme: string;

// Insert: adicionar
theme?: string;

// Update: adicionar
theme?: string;
```

- [ ] **Step 4: Verificar TypeScript**

```bash
npm run type-check 2>&1 | tail -5
```

Esperado: `0 errors`.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260318000001_add_theme_to_consultants.sql src/types/database.ts
git commit -m "feat(skins): add theme column migration and update database types"
```

---

## Task 5: PATCH /api/consultants/me endpoint

**Files:**

- Create: `src/app/api/consultants/me/route.ts`
- Test: `tests/unit/app/api/consultants/me/route.test.ts`

- [ ] **Step 1: Escrever o teste (TDD — vai falhar)**

Criar `tests/unit/app/api/consultants/me/route.test.ts`:

```typescript
import { NextRequest } from 'next/server';
import { beforeEach, describe, expect, it, vi } from 'vitest';

// Mock Supabase
const mockSupabase = {
  auth: { getUser: vi.fn() },
  from: vi.fn().mockReturnThis(),
  update: vi.fn().mockReturnThis(),
  eq: vi.fn().mockReturnThis(),
  select: vi.fn().mockReturnThis(),
  single: vi.fn(),
};

vi.mock('@/lib/supabase/server', () => ({
  createClient: vi.fn().mockResolvedValue(mockSupabase),
}));

async function makeRequest(body: unknown, method = 'PATCH') {
  const { PATCH } = await import('./route');
  const req = new NextRequest(new URL('http://localhost/api/consultants/me'), {
    method,
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(body),
  });
  return PATCH(req);
}

describe('PATCH /api/consultants/me', () => {
  beforeEach(() => {
    vi.clearAllMocks();
    vi.resetModules();
  });

  it('returns 401 when not authenticated', async () => {
    mockSupabase.auth.getUser.mockResolvedValue({ data: { user: null } });
    const res = await makeRequest({ theme: 'noturno' });
    expect(res.status).toBe(401);
  });

  it('updates theme for authenticated user', async () => {
    mockSupabase.auth.getUser.mockResolvedValue({ data: { user: { id: 'user-1' } } });
    mockSupabase.single.mockResolvedValue({ data: { theme: 'noturno' }, error: null });
    const res = await makeRequest({ theme: 'noturno' });
    expect(res.status).toBe(200);
    const json = await res.json();
    expect(json.data.theme).toBe('noturno');
  });

  it('returns 400 for invalid theme value', async () => {
    mockSupabase.auth.getUser.mockResolvedValue({ data: { user: { id: 'user-1' } } });
    const res = await makeRequest({ theme: 'invalid-theme' });
    expect(res.status).toBe(400);
  });

  it('returns 400 when theme field is missing', async () => {
    mockSupabase.auth.getUser.mockResolvedValue({ data: { user: { id: 'user-1' } } });
    const res = await makeRequest({});
    expect(res.status).toBe(400);
  });
});
```

- [ ] **Step 2: Rodar o teste — deve falhar**

```bash
cd E:/PROJETOS/Consultor.AI && npm run test:unit -- --reporter=verbose tests/unit/app/api/consultants/me/route.test.ts
```

Esperado: FAIL — `Cannot find module './route'`.

- [ ] **Step 3: Criar `src/app/api/consultants/me/route.ts`**

```typescript
import { type NextRequest, NextResponse } from 'next/server';
import { z } from 'zod';
import { createClient } from '@/lib/supabase/server';

const VALID_THEMES = ['corporate', 'noturno', 'moderno', 'classico'] as const;

const updateSchema = z.object({
  theme: z.enum(VALID_THEMES),
});

export async function PATCH(req: NextRequest) {
  try {
    const supabase = await createClient();
    const {
      data: { user },
    } = await supabase.auth.getUser();

    if (!user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
    }

    const body = await req.json();
    const parsed = updateSchema.safeParse(body);

    if (!parsed.success) {
      return NextResponse.json(
        { error: 'Invalid theme value', details: parsed.error.flatten() },
        { status: 400 }
      );
    }

    const { data, error } = await (supabase as any)
      .from('consultants')
      .update({ theme: parsed.data.theme })
      .eq('user_id', user.id)
      .select('theme')
      .single();

    if (error) {
      return NextResponse.json({ error: error.message }, { status: 500 });
    }

    return NextResponse.json({ data });
  } catch {
    return NextResponse.json({ error: 'Internal server error' }, { status: 500 });
  }
}
```

- [ ] **Step 4: Rodar o teste — deve passar**

```bash
npm run test:unit -- --reporter=verbose tests/unit/app/api/consultants/me/route.test.ts
```

Esperado: 4 tests PASS.

- [ ] **Step 5: Commit**

```bash
git add src/app/api/consultants/me/route.ts tests/unit/app/api/consultants/me/route.test.ts
git commit -m "feat(skins): add PATCH /api/consultants/me endpoint with tests"
```

---

## Task 6: Root layout.tsx — SSR anti-flash

**Files:**

- Modify: `src/app/layout.tsx`

- [ ] **Step 1: Atualizar `src/app/layout.tsx`**

Substituir o arquivo pelo seguinte (preservar `metadata` e `inter` existentes):

```tsx
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import { cookies } from 'next/headers';
import './globals.css';
import { Providers } from '@/components/providers';
import { isValidSkinId } from '@/lib/skin/types';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Consultor.AI - AI-Powered WhatsApp Sales Assistant',
  description:
    'Automatize a qualificação de leads e agendamentos via WhatsApp com assistentes de IA personalizados',
  keywords: [
    'WhatsApp',
    'Sales Assistant',
    'Lead Qualification',
    'AI',
    'Automation',
    'Plano de Saúde',
  ],
  authors: [{ name: 'Consultor.AI Team' }],
  creator: 'Consultor.AI',
  metadataBase: new URL(process.env.NEXT_PUBLIC_APP_URL || 'http://localhost:3000'),
  openGraph: {
    type: 'website',
    locale: 'pt_BR',
    url: '/',
    title: 'Consultor.AI',
    description: 'Assistente de vendas por WhatsApp com IA',
    siteName: 'Consultor.AI',
  },
  twitter: {
    card: 'summary_large_image',
    title: 'Consultor.AI',
    description: 'Assistente de vendas por WhatsApp com IA',
  },
  robots: { index: true, follow: true },
};

export default async function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  const cookieStore = await cookies();
  const rawSkin = cookieStore.get('skin')?.value;
  // Sanitizar: rejeitar valores inválidos/tampered antes de injetar no HTML
  const skin = isValidSkinId(rawSkin) ? rawSkin : 'corporate';

  return (
    <html
      lang="pt-BR"
      data-skin={skin}
      className={skin === 'noturno' ? 'dark' : ''}
      suppressHydrationWarning
    >
      <body className={inter.className}>
        <Providers initialSkin={skin}>{children}</Providers>
      </body>
    </html>
  );
}
```

> Nota: `SkinProvider` precisa aceitar `initialSkin` como prop. Atualizar `providers.tsx` para repassar o prop ao `SkinProvider` (já previsto no Task 3 Step 2).

- [ ] **Step 2: Verificar que `Providers` já aceita `initialSkin`**

O prop `initialSkin` foi adicionado em Task 3 Step 2. Verificar que o `providers.tsx` está correto e nenhum TypeScript error existe:

```bash
npm run type-check 2>&1 | grep providers
```

Esperado: nenhum erro em providers.tsx.

- [ ] **Step 3: Verificar TypeScript + build**

```bash
npm run type-check 2>&1 | tail -5
```

Esperado: `0 errors`.

- [ ] **Step 4: Commit**

```bash
git add src/app/layout.tsx src/components/providers.tsx
git commit -m "feat(skins): update root layout for SSR skin cookie injection"
```

---

## Task 7: SkinSwitcher component no header

**Files:**

- Create: `src/components/ui/skin-switcher.tsx`
- Modify: `src/app/dashboard/layout.tsx`
- Test: `tests/unit/components/ui/skin-switcher.test.tsx`

- [ ] **Step 1: Escrever o teste**

Criar `tests/unit/components/ui/skin-switcher.test.tsx`:

```tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { describe, expect, it, vi } from 'vitest';
import { SkinSwitcher } from '@/components/ui/skin-switcher';

const mockSetSkin = vi.fn();
vi.mock('@/lib/skin/skin-context', () => ({
  useSkin: () => ({
    skinId: 'corporate',
    setSkin: mockSetSkin,
    skins: [
      { id: 'corporate', name: 'Corporate', icon: '🏢', description: 'Profissional' },
      { id: 'noturno', name: 'Noturno', icon: '🌙', description: 'Modo escuro' },
      { id: 'moderno', name: 'Moderno', icon: '⚡', description: 'Clean' },
      { id: 'classico', name: 'Clássico', icon: '📜', description: 'Tradicional' },
    ],
  }),
}));

describe('SkinSwitcher', () => {
  it('renderiza o botão trigger', () => {
    render(<SkinSwitcher />);
    expect(screen.getByRole('button')).toBeDefined();
  });

  it('abre o dropdown e mostra as 4 skins', async () => {
    render(<SkinSwitcher />);
    fireEvent.click(screen.getByRole('button'));
    expect(screen.getByText('Corporate')).toBeDefined();
    expect(screen.getByText('Noturno')).toBeDefined();
    expect(screen.getByText('Moderno')).toBeDefined();
    expect(screen.getByText('Clássico')).toBeDefined();
  });

  it('chama setSkin ao clicar em uma opção', async () => {
    render(<SkinSwitcher />);
    fireEvent.click(screen.getByRole('button'));
    fireEvent.click(screen.getByText('Noturno'));
    expect(mockSetSkin).toHaveBeenCalledWith('noturno');
  });
});
```

- [ ] **Step 2: Rodar o teste — deve falhar**

```bash
npm run test:unit -- --reporter=verbose tests/unit/components/ui/skin-switcher.test.tsx
```

Esperado: FAIL — `Cannot find module`.

- [ ] **Step 3: Criar `src/components/ui/skin-switcher.tsx`**

```tsx
'use client';

import { Check, Palette } from 'lucide-react';
import { useSkin } from '@/lib/skin/skin-context';
import { Button } from '@/components/ui/button';
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuLabel,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from '@/components/ui/dropdown-menu';

export function SkinSwitcher() {
  const { skinId, setSkin, skins } = useSkin();

  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <Button variant="ghost" size="icon" title="Alterar aparência">
          <Palette className="h-5 w-5" />
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end">
        <DropdownMenuLabel>Aparência</DropdownMenuLabel>
        <DropdownMenuSeparator />
        {skins.map(skin => (
          <DropdownMenuItem
            key={skin.id}
            onClick={() => setSkin(skin.id)}
            className="flex cursor-pointer items-center gap-2"
          >
            <span>{skin.icon}</span>
            <span className="flex-1">{skin.name}</span>
            {skinId === skin.id && <Check className="h-4 w-4 text-primary" />}
          </DropdownMenuItem>
        ))}
      </DropdownMenuContent>
    </DropdownMenu>
  );
}
```

- [ ] **Step 4: Adicionar `<SkinSwitcher>` ao dashboard layout**

Em `src/app/dashboard/layout.tsx`, importar e adicionar `<SkinSwitcher>` na área do `<UserMenu>`. Localizar o `<UserMenu />` no final do aside e adicionar o switcher em um topbar ou ao lado. Como o layout atual não tem um topbar visível, adicionar dentro do `<aside>`, logo acima do `<UserMenu>`:

```tsx
// Adicionar import
import { SkinSwitcher } from '@/components/ui/skin-switcher';

// No aside, antes do UserMenu, adicionar:
<div className="flex items-center justify-between border-t border-border px-4 py-2">
  <span className="text-xs text-muted-foreground">Aparência</span>
  <SkinSwitcher />
</div>;
```

- [ ] **Step 5: Rodar o teste — deve passar**

```bash
npm run test:unit -- --reporter=verbose tests/unit/components/ui/skin-switcher.test.tsx
```

Esperado: 3 tests PASS.

- [ ] **Step 6: Commit**

```bash
git add src/components/ui/skin-switcher.tsx tests/unit/components/ui/skin-switcher.test.tsx src/app/dashboard/layout.tsx
git commit -m "feat(skins): add SkinSwitcher component to dashboard header"
```

---

## Task 8: Seção "Aparência" na página de perfil

**Files:**

- Modify: `src/app/dashboard/perfil/page.tsx`

- [ ] **Step 1: Adicionar seção Aparência à `perfil/page.tsx`**

No final do `<div className="grid gap-6">`, antes do fechamento, adicionar:

```tsx
// Adicionar import no topo do arquivo (SkinSwitcher NÃO é usado aqui — não importar)
import { useSkin } from '@/lib/skin/skin-context';

// Dentro do componente, após as declarações existentes
const { skinId, setSkin, skins } = useSkin();

// No JSX, adicionar como último card dentro do grid:
{
  /* Aparência */
}
<div className="rounded-lg border border-border bg-card p-6 shadow">
  <h2 className="mb-4 text-xl font-semibold text-foreground">Aparência</h2>
  <p className="mb-4 text-sm text-muted-foreground">Escolha o tema visual da interface</p>
  <div className="grid grid-cols-2 gap-3">
    {skins.map(skin => (
      <button
        key={skin.id}
        onClick={() => setSkin(skin.id)}
        className={`relative flex flex-col items-start gap-2 rounded-lg border-2 p-4 text-left transition-colors hover:bg-muted ${
          skinId === skin.id ? 'border-primary bg-primary/5' : 'border-border'
        }`}
      >
        {/* Mini preview */}
        <div
          className="h-10 w-full rounded"
          style={{ backgroundColor: skin.previewBg, border: `3px solid ${skin.previewPrimary}` }}
        />
        <div className="flex items-center gap-2">
          <span>{skin.icon}</span>
          <span className="text-sm font-medium text-foreground">{skin.name}</span>
        </div>
        <p className="text-xs text-muted-foreground">{skin.description}</p>
        {skinId === skin.id && (
          <span className="absolute right-2 top-2 rounded-full bg-primary px-2 py-0.5 text-xs font-medium text-primary-foreground">
            Ativo
          </span>
        )}
      </button>
    ))}
  </div>
</div>;
```

- [ ] **Step 2: Verificar TypeScript**

```bash
npm run type-check 2>&1 | tail -5
```

Esperado: `0 errors`.

- [ ] **Step 3: Commit**

```bash
git add src/app/dashboard/perfil/page.tsx
git commit -m "feat(skins): add Aparência section to perfil page"
```

---

## Task 9: Refatoração de componentes — remover hardcoded colors

**Regra geral de substituição:**

| Remover                                    | Substituir por                             |
| ------------------------------------------ | ------------------------------------------ |
| `bg-white`                                 | `bg-background` ou `bg-card` (se for card) |
| `bg-gray-50` / `bg-gray-100`               | `bg-muted`                                 |
| `bg-gray-800`, `dark:bg-gray-800`          | `bg-card`                                  |
| `bg-gray-900`, `dark:bg-gray-900`          | `bg-background`                            |
| `text-gray-900`, `dark:text-white`         | `text-foreground`                          |
| `text-gray-700`, `dark:text-gray-300`      | `text-foreground`                          |
| `text-gray-600`, `dark:text-gray-400`      | `text-muted-foreground`                    |
| `text-gray-500`, `dark:text-gray-400`      | `text-muted-foreground`                    |
| `border-gray-200`, `dark:border-gray-700`  | `border-border`                            |
| `bg-gray-200 dark:bg-gray-700` (skeleton)  | `bg-muted`                                 |
| `hover:bg-gray-100 dark:hover:bg-gray-700` | `hover:bg-muted`                           |

**Files:**

- Modify: `src/app/dashboard/layout.tsx`
- Modify: `src/app/dashboard/page.tsx`
- Modify: `src/app/dashboard/perfil/page.tsx`
- Modify: `src/components/auth/UserMenu.tsx`
- Modify: `src/components/dashboard/metric-card.tsx`
- Modify: `src/components/leads/lead-card.tsx`
- Modify: `src/components/leads/lead-list.tsx`
- Modify: `src/components/leads/lead-filters.tsx`
- Modify: `src/components/admin/admin-sidebar.tsx`
- Modify: `src/components/admin/stats-card.tsx`
- Modify: `src/components/billing/pricing-card.tsx`

- [ ] **Step 1: Refatorar `src/app/dashboard/layout.tsx`**

Aplicar a tabela de substituição. Principais trocas:

- `bg-gray-50 dark:bg-gray-900` → `bg-muted`
- `bg-white dark:bg-gray-800` → `bg-card`
- `border-r` (já usa `border-border` via `@layer base`) — OK
- `dark:border-gray-700` → remover (border-border resolve)
- `text-gray-700 dark:text-gray-300` → `text-foreground`
- `hover:bg-gray-100 dark:hover:bg-gray-700` → `hover:bg-muted`
- `text-gray-900 dark:text-white` → `text-foreground`

- [ ] **Step 2: Refatorar `src/app/dashboard/page.tsx`**

São ~30 ocorrências. Seguir a tabela. Atenção especial:

- Os cards de status usam cores semânticas (`bg-blue-100 text-blue-700`) — **manter** essas (são cores de status, não de tema)
- Remover apenas os pares `dark:` que correspondem a tokens de tema (background, foreground, border)

- [ ] **Step 3: Refatorar `src/app/dashboard/perfil/page.tsx`**

- `rounded-lg bg-white p-6 shadow dark:bg-gray-800` → `rounded-lg bg-card p-6 shadow border border-border`
- `text-gray-600 dark:text-gray-400` → `text-muted-foreground`
- `bg-gray-50 dark:bg-gray-700` → `bg-muted`
- `text-gray-500 dark:text-gray-400` → `text-muted-foreground`

- [ ] **Step 4: Refatorar `src/components/auth/UserMenu.tsx`**

- `dark:bg-gray-700` → `bg-muted`
- `dark:border-gray-700` → remover (border-border via base)
- `text-gray-900 dark:text-white` → `text-foreground`
- `text-gray-500 dark:text-gray-400` → `text-muted-foreground`

- [ ] **Step 5: Refatorar `src/components/dashboard/metric-card.tsx`**

Ler o arquivo primeiro. Aplicar substituições padrão.

- [ ] **Step 6: Refatorar componentes de leads**

```bash
# Verificar ocorrências
grep -n "dark:" src/components/leads/lead-card.tsx src/components/leads/lead-list.tsx src/components/leads/lead-filters.tsx
```

Aplicar substituições padrão em cada arquivo.

- [ ] **Step 7: Refatorar componentes de admin e billing**

```bash
grep -n "dark:" src/components/admin/admin-sidebar.tsx src/components/admin/stats-card.tsx src/components/billing/pricing-card.tsx
```

Aplicar substituições padrão.

- [ ] **Step 8: Verificar que não restam dark: variants nos componentes refatorados**

```bash
grep -rn "dark:bg-gray\|dark:text-gray\|dark:border-gray" src/app/dashboard/ src/components/auth/ src/components/dashboard/ src/components/leads/ src/components/admin/ src/components/billing/
```

Esperado: nenhuma ocorrência.

- [ ] **Step 9: Verificar TypeScript**

```bash
npm run type-check 2>&1 | tail -5
```

Esperado: `0 errors`.

- [ ] **Step 10: Rodar todos os testes existentes**

```bash
npm run test:unit 2>&1 | tail -10
```

Esperado: todos passing (319 tests).

- [ ] **Step 11: Commit A — layouts e páginas do dashboard**

> Nota: `perfil/page.tsx` já foi commitado em Task 8 — NÃO incluir aqui.

```bash
git add src/app/dashboard/layout.tsx src/app/dashboard/page.tsx
git commit -m "refactor(skins): replace hardcoded colors in dashboard layout and main page"
```

- [ ] **Step 12: Commit B — componentes**

```bash
git add src/components/auth/UserMenu.tsx src/components/dashboard/ src/components/leads/ src/components/admin/ src/components/billing/
git commit -m "refactor(skins): replace hardcoded dark: variants in UI components"
```

---

## Task 10: Testes do SkinContext + E2E

**Files:**

- Create: `tests/unit/lib/skin/skin-context.test.tsx`
- Create: `tests/e2e/skin-system.spec.ts`

- [ ] **Step 1: Criar `tests/unit/lib/skin/skin-context.test.tsx`**

```typescript
import { describe, expect, it, vi, beforeEach } from 'vitest';
import { renderHook, act } from '@testing-library/react';
import { SkinProvider, useSkin } from '@/lib/skin/skin-context';

// Mock fetch
global.fetch = vi.fn().mockResolvedValue({ ok: true });

// Mock localStorage
const localStorageMock = (() => {
  let store: Record<string, string> = {};
  return {
    getItem: (key: string) => store[key] ?? null,
    setItem: (key: string, val: string) => { store[key] = val; },
    clear: () => { store = {}; },
  };
})();
Object.defineProperty(window, 'localStorage', { value: localStorageMock });

describe('useSkin', () => {
  beforeEach(() => {
    localStorageMock.clear();
    vi.clearAllMocks();
    document.documentElement.removeAttribute('data-skin');
    document.documentElement.classList.remove('dark');
  });

  it('inicia com skin corporate por padrão', () => {
    const { result } = renderHook(() => useSkin(), {
      wrapper: ({ children }) => <SkinProvider>{children}</SkinProvider>,
    });
    expect(result.current.skinId).toBe('corporate');
  });

  it('setSkin aplica data-skin no html', async () => {
    const { result } = renderHook(() => useSkin(), {
      wrapper: ({ children }) => <SkinProvider>{children}</SkinProvider>,
    });
    await act(async () => {
      await result.current.setSkin('noturno');
    });
    expect(document.documentElement.getAttribute('data-skin')).toBe('noturno');
  });

  it('setSkin noturno adiciona classe .dark', async () => {
    const { result } = renderHook(() => useSkin(), {
      wrapper: ({ children }) => <SkinProvider>{children}</SkinProvider>,
    });
    await act(async () => {
      await result.current.setSkin('noturno');
    });
    expect(document.documentElement.classList.contains('dark')).toBe(true);
  });

  it('setSkin moderno remove classe .dark', async () => {
    document.documentElement.classList.add('dark');
    const { result } = renderHook(() => useSkin(), {
      wrapper: ({ children }) => <SkinProvider>{children}</SkinProvider>,
    });
    await act(async () => {
      await result.current.setSkin('moderno');
    });
    expect(document.documentElement.classList.contains('dark')).toBe(false);
  });

  it('setSkin salva no localStorage', async () => {
    const { result } = renderHook(() => useSkin(), {
      wrapper: ({ children }) => <SkinProvider>{children}</SkinProvider>,
    });
    await act(async () => {
      await result.current.setSkin('classico');
    });
    expect(localStorageMock.getItem('consultor-ai-skin')).toBe('classico');
  });

  it('setSkin chama PATCH /api/consultants/me', async () => {
    const { result } = renderHook(() => useSkin(), {
      wrapper: ({ children }) => <SkinProvider>{children}</SkinProvider>,
    });
    await act(async () => {
      await result.current.setSkin('moderno');
    });
    expect(fetch).toHaveBeenCalledWith(
      '/api/consultants/me',
      expect.objectContaining({ method: 'PATCH' })
    );
  });
});
```

- [ ] **Step 2: Rodar os testes unitários**

```bash
npm run test:unit -- --reporter=verbose tests/unit/lib/skin/skin-context.test.tsx
```

Esperado: 6 tests PASS.

- [ ] **Step 3: Criar `tests/e2e/skin-system.spec.ts`**

```typescript
import { expect, test } from '@playwright/test';

test.describe('Sistema de Skins', () => {
  test.beforeEach(async ({ page }) => {
    // Garantir que começa autenticado (usa o auth setup do projeto)
    await page.goto('/dashboard');
    await page.waitForURL('**/dashboard**');
  });

  test('página carrega com skin corporate por padrão', async ({ page }) => {
    const html = page.locator('html');
    await expect(html).toHaveAttribute('data-skin', 'corporate');
  });

  test('trocar skin via header atualiza data-skin imediatamente', async ({ page }) => {
    // Abrir o dropdown do SkinSwitcher
    await page.getByTitle('Alterar aparência').click();
    // Clicar em Noturno
    await page.getByText('Noturno').click();
    // Verificar que data-skin mudou
    const html = page.locator('html');
    await expect(html).toHaveAttribute('data-skin', 'noturno');
  });

  test('skin persiste após recarregar a página', async ({ page }) => {
    // Trocar para Moderno
    await page.getByTitle('Alterar aparência').click();
    await page.getByText('Moderno').click();
    // Recarregar
    await page.reload();
    await page.waitForURL('**/dashboard**');
    // Verificar persistência via cookie SSR
    const html = page.locator('html');
    await expect(html).toHaveAttribute('data-skin', 'moderno');
  });

  test('skin Noturno adiciona classe dark ao html', async ({ page }) => {
    await page.getByTitle('Alterar aparência').click();
    await page.getByText('Noturno').click();
    const html = page.locator('html');
    await expect(html).toHaveClass(/dark/);
  });
});
```

- [ ] **Step 4: Rodar todos os testes unitários para garantir nenhuma regressão**

```bash
npm run test:unit 2>&1 | tail -5
```

Esperado: todos passing.

- [ ] **Step 5: Commit final**

```bash
git add tests/unit/lib/skin/ tests/e2e/skin-system.spec.ts
git commit -m "test(skins): add unit tests for SkinContext and E2E skin persistence tests"
```

---

## Task 11: Verificação final

- [ ] **Step 1: Build de produção**

```bash
npm run build 2>&1 | tail -10
```

Esperado: build sem erros.

- [ ] **Step 2: Type check limpo**

```bash
npm run type-check 2>&1 | tail -5
```

Esperado: `0 errors`.

- [ ] **Step 3: Todos os testes passando**

```bash
npm test 2>&1 | tail -10
```

Esperado: todos os testes passando (≥ 329 tests — 319 existentes + ~10 novos).

- [ ] **Step 4: Verificar grep de dark: variants remanescentes**

```bash
grep -rn "dark:bg-gray\|dark:text-gray\|dark:border-gray" src/ --include="*.tsx" --include="*.ts" | grep -v "node_modules"
```

Esperado: nenhuma ocorrência nos arquivos refatorados.

- [ ] **Step 5: Commit de release**

```bash
git add -A
git commit -m "feat(skins): complete skin system implementation

- 4 skins: Corporate, Noturno, Moderno, Clássico
- CSS variables + data-skin attribute
- SkinContext with localStorage + DB persistence
- SkinSwitcher in dashboard header
- Aparência section in perfil page
- PATCH /api/consultants/me endpoint
- Full component refactor (dark: variants → CSS variables)
- Unit + E2E tests"
```
